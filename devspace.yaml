version: v1beta3
images:
  webserver:
    image: dscr.io/slevine111/worldofchatting
    createPullSecret: true
deployments:
  - name: worldofchatting
    kubectl:
      manifests:
        - k8s-common
        - k8s-prod
#dev:
# ports:
#  - imageName: webserver
#   forward:
#    - port: 3000
#sync:
# - imageName: webserver
#  excludePaths:
#   - .vscode
#  - .devspace
#  - coverage
# - k8s-common
# - k8s-dev
# - k8s-prod
# - test-results
# - .circleci
# - npm-debug.log
# - .git
# - .gitignore
# - .dockerignore
# - Dockerfile
# - Dockerfile-test
# - devspace.yaml
